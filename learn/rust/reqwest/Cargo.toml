[package]
name = "reqwest"
version = "0.1.0"
edition = "2021"

[dependencies]
reqwest = { version = "0.11", features = [
    "blocking",
], default-features = false }
serde = { version = "1.0", features = ["derive"] }
